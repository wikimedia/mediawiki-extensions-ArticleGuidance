{
	"name": "ArticleGuidance",
	"private": true,
	"scripts": {
		"test": "npm -s run lint && jest",
		"lint": "npm -s run lint:i18n && npm -s run lint:js && npm -s run lint:css",
		"lint:fix": "npm -s run lint:i18n && npm -s run lint:js -- --fix && npm -s run lint:css -- --fix",
		"lint:css": "stylelint --cache --allow-empty-input \"**/*.less\"",
		"lint:i18n": "banana-checker --requireLowerCase=0 i18n/",
		"lint:js": "eslint --cache ."
	},
	"devDependencies": {
		"eslint-config-wikimedia": "0.32.4",
		"grunt-banana-checker": "0.13.0",
		"jest": "30.3.0",
		"jest-environment-jsdom": "30.3.0",
		"stylelint-config-wikimedia": "0.18.0"
	},
	"eslintIgnore": [
		"vendor/**"
	]
}
